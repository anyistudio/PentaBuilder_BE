Function create_llm_client:
- select the concrete provider adapter from Settings
- return GeminiClient when Google is configured
- return OpenAIClient when OpenAI is configured
- return None when no live provider is configured so the service layer can fail fast with provider_not_configured
