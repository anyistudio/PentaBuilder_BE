TypedDict RunGraphState:
- hold context, payload, prompt, model candidate result, final validated result, and streamed preview text
- track bounded tool-planning state including rounds, call count, tool_trace, tool_facts, and dedupe keys
- accumulate provider usage across tool planning, final generation, and optional repair calls
